server:
  port: 8083

spring:
  application:
    name: co-optimization-service
  datasource:
    url: jdbc:mysql://localhost:3306/marl_tsc?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: yph20051018
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 10000

# MyBatis Configuration
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.njhtr.marltsc.coopt.domain.entity
